!= js('topic_graph')
header
  style
    circle.node {
      stroke: #fff;
      stroke-width: 1.5px;
    }

    line.link {
      stroke: #999;
      stroke-opacity: .6;
    }
body
  #chart
  script
    $(function() { window.TopicGraph.init('#{data_path}') })